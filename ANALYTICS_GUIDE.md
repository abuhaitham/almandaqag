# دليل تحليلات الموقع

## نظرة عامة
تم إضافة نظام تحليلات شامل لتتبع زيارات الموقع ونشاط المستخدمين. يوفر النظام إحصائيات مفصلة ورسوم بيانية تفاعلية.

## الميزات المضافة

### 1. تتبع الزيارات
- تتبع تلقائي لجميع زيارات الصفحات
- حفظ معلومات الجلسة والمتصفح
- تتبع المصادر المرجعية (Referrers)

### 2. لوحة التحليلات
- **إحصائيات عامة**: إجمالي الزيارات، الزوار الفريدون، عدد الصفحات
- **الرسوم البيانية**:
  - الرسم البياني اليومي للزيارات
  - الرسم البياني الساعي للنشاط
  - أكثر الصفحات زيارة
  - أنواع الأجهزة المستخدمة
  - مصادر الزيارات

### 3. التقارير التفاعلية
- إمكانية تغيير الفترة الزمنية (7، 30، 90 يوم)
- تصدير البيانات
- تحديث البيانات في الوقت الفعلي

## كيفية الوصول

### للمديرين
1. تسجيل الدخول إلى لوحة التحكم الإدارية
2. النقر على تبويب "📊 تحليلات الموقع"
3. النقر على "عرض التحليلات التفصيلية"

### الصفحة المباشرة
- `/analytics` - صفحة التحليلات الكاملة (تتطلب تسجيل دخول إداري)

## البيانات المتتبعة

### معلومات الزيارة
- **الصفحة**: اسم الصفحة التي تمت زيارتها
- **المتصفح**: معلومات User Agent
- **عنوان IP**: عنوان IP للزائر
- **المصدر المرجعي**: الصفحة التي جاء منها الزائر
- **معرف الجلسة**: معرف فريد للجلسة
- **التاريخ والوقت**: وقت الزيارة

### الإحصائيات المحسوبة
- **إجمالي الزيارات**: عدد جميع الزيارات
- **الزوار الفريدون**: عدد الجلسات الفريدة
- **مشاهدات الصفحات**: عدد مرات زيارة كل صفحة
- **الزيارات اليومية**: توزيع الزيارات حسب اليوم
- **الزيارات الساعية**: توزيع الزيارات حسب الساعة
- **أكثر الصفحات زيارة**: ترتيب الصفحات حسب عدد الزيارات
- **أنواع الأجهزة**: توزيع الزيارات حسب نوع الجهاز (Desktop, Mobile, Tablet)
- **مصادر الزيارات**: المواقع التي جاء منها الزوار

## التقنيات المستخدمة

### المكتبات
- **Recharts**: لإنشاء الرسوم البيانية التفاعلية
- **date-fns**: لمعالجة التواريخ والأوقات
- **Supabase**: قاعدة البيانات وتخزين البيانات

### المكونات
- `AnalyticsDashboard`: مكون لوحة التحليلات الرئيسي
- `AnalyticsPage`: صفحة التحليلات الكاملة
- `lib/analytics.ts`: وظائف تتبع الزيارات وجلب البيانات

## قاعدة البيانات

### جدول website_visits
```sql
CREATE TABLE website_visits (
  id SERIAL PRIMARY KEY,
  page VARCHAR(255) NOT NULL,
  user_agent TEXT,
  ip_address INET,
  referrer TEXT,
  session_id VARCHAR(255) NOT NULL,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

### الفهارس
- `idx_website_visits_created_at`: للبحث حسب التاريخ
- `idx_website_visits_page`: للبحث حسب الصفحة
- `idx_website_visits_session_id`: للبحث حسب الجلسة

## الأمان والخصوصية

### حماية البيانات
- لا يتم حفظ معلومات شخصية حساسة
- عناوين IP محفوظة للتحليل الإحصائي فقط
- إمكانية حذف البيانات القديمة

### التحكم في الوصول
- التحليلات متاحة للمديرين فقط
- تتطلب تسجيل دخول إداري
- حماية من الوصول غير المصرح به

## الصيانة والتطوير

### تحديث البيانات
- البيانات تُحدث تلقائياً عند كل زيارة
- إمكانية التحديث اليدوي من لوحة التحكم
- معالجة الأخطاء والتسجيل

### الأداء
- فهرسة قاعدة البيانات لتحسين الأداء
- تحميل البيانات بشكل تدريجي
- تحسين حجم الرسوم البيانية

## استكشاف الأخطاء

### مشاكل شائعة
1. **لا تظهر البيانات**: تأكد من وجود بيانات في جدول website_visits
2. **خطأ في التحميل**: تحقق من اتصال قاعدة البيانات
3. **الرسوم البيانية لا تظهر**: تأكد من تحميل مكتبة Recharts

### سجلات الأخطاء
- يتم تسجيل الأخطاء في console المتصفح
- فحص سجلات Supabase للتحقق من الأخطاء
- مراقبة أداء الاستعلامات

## التطوير المستقبلي

### ميزات مقترحة
- تصدير التقارير بصيغة PDF
- تنبيهات عند زيادة الزيارات
- تحليل سلوك المستخدمين
- تتبع مسارات التنقل
- تحليل الكلمات المفتاحية
- تكامل مع Google Analytics

### التحسينات
- تحسين أداء الاستعلامات
- إضافة المزيد من أنواع الرسوم البيانية
- تحسين واجهة المستخدم
- إضافة فلاتر متقدمة
